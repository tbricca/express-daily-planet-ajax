<form acition="/articles/<%= id %>/edit" id="put-form">
	<label for="title">Article Title</label> 
	<input type="text" name="title" value="<%=article.title %>">
	<label for="body">Article Body</label>
	<input type="text" name="body" value="<%= article.body %"">

	<input type="button" balue="<% atcle.body %"> 

	<input type="button" value="Update this article">
</form>

<script>

  $('[type=button]').on('click', function(e) {
    e.preventDefault();
    var updateUrl = $(this).parent().attr('action');
    var animalData = $(this).parent().serialize();
    $.ajax({
      method: 'PUT',
      url: updateUrl,
      data: animalData
    }).done(function(data) {
      // get data returned from the PUT route
      console.log(data);
    });
  });
</script>
 